{% extends 'base.html.twig' %}

{% block title %}Ajout d'un Client - Formulaire externalisé{% endblock %}
{% block body %}
    <div class="d-flex justify-content-center" >
        {{ form_start(my_form) }}
        {{ form_errors(my_form) }}
        {# <div class="form-group">
            {{ form_label(my_form.nomAcheteur) }}
            {{ form_widget(my_form.nomAcheteur, {'attr': { 'class':'form-control'}}) }}
            {{ form_errors(my_form.nomAcheteur) }}
        </div>
        <div class="form-group3">
            {{ form_row(my_form.prenomAcheteur, {'attr': { 'class':'form-control'}}) }}
        </div> #}
        {{ form_end(my_form) }}
    </div> 
{% endblock %}